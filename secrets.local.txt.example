# Copy this file to `secrets.local.txt` and add the real terms, one per line.
# `secrets.local.txt` is git-ignored. This example file is not, so keep it empty of
# anything real.
#
# List the sponsoring company, its partners, and any product or project code names.
# `npm run check:confidential` scans every text file and the last 200 commit messages
# for these terms.
#
# Example:
#   Northwind Systems
#   Northwind
#   Project Kestrel
